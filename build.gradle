plugins {
    id 'java'
    id("maven-publish")
}

final author = 'Gudao Luo'
final artifactId = 'gradle_java'
final sourceCompatibility = JavaVersion.VERSION_11
group = "de.iotoi"
version = '1.1.1'

println project.findProperty("gpr.user")

repositories {
    jcenter()
}

dependencies {
}


publishing {
    repositories {
        maven {
            name = "nullPackage"
            url = uri("https://github.com/cnruby/gradle_java")
            credentials {
                username = project.findProperty("gpr.user")
                password = project.findProperty("gpr.key")
            }
        }
    }
    publications {
        gpr(MavenPublication) {
            from(components.java)
        }
    }
}